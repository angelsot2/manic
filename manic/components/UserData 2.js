

class User {
    constructor(id, name, phoneNumber, email) {
        this.id = id;
        this.name = name;
        this.phoneNumber;
        this.email = email;
    }
}

export default function UserFunctions() {

    //create an instance of the friends list
    const createFriendsList = () => {
        {/* should be a list in the sql database */}
    }

    //this will be a list of the work a user has posted 
    const postedWork = () => {
    }

}

{/*TODO: 
    - create friends list datatype 
    - import authentication package to enable login/logout functionality 
*/}
