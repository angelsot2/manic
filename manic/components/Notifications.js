{/* This file contains data to push notifications to user's device */}

export default function Notifications() {
    
    const pushNotification = () => {
    }

    const reminderNotification = () => {
    }
}


{/*TODO:
    - Import package to push notificaions to device 
        - need to ask device for permission (also import package)
*/}
